function soma(a, b) {
    return a + b;
}

function subtrai(a, b) {
    const resultado = a - b;
    return resultado;
}

const resultado = soma(10, 20);
console.log(resultado);

const result = subtrai(30, 5);
console.log(result);